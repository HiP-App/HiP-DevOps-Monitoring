

groups:
- name: targets
  rules:
  - alert: monitor_service_down
    expr: up == 0
    for: 30s
    labels:
      severity: critical
    annotations:
      summary: "Monitor service non-operational"
      description: "Service {{ $labels.instance }} is down."

- name: host
  rules:
  - alert: high_cpu_load
    expr: node_load1 > 1.5
    for: 30s
    labels:
      severity: warning
    annotations:
      summary: "Server under high load"
      description: "Docker host is under high load, the avg load 1m is at {{ $value}}. Reported by instance {{ $labels.instance }} of job {{ $labels.job }}."

  - alert: high_memory_load
    expr: (sum(node_memory_MemTotal) - sum(node_memory_MemFree + node_memory_Buffers + node_memory_Cached) ) / sum(node_memory_MemTotal) * 100 > 85
    for: 30s
    labels:
      severity: warning
    annotations:
      summary: "Server memory is almost full"
      description: "Docker host memory usage is {{ humanize $value}}%. Reported by instance {{ $labels.instance }} of job {{ $labels.job }}."

  - alert: high_storage_load
    expr: (node_filesystem_size{fstype="aufs"} - node_filesystem_free{fstype="aufs"}) / node_filesystem_size{fstype="aufs"}  * 100 > 85
    for: 30s
    labels:
      severity: warning
    annotations:
      summary: "Server storage is almost full"
      description: "Docker host storage usage is {{ humanize $value}}%. Reported by instance {{ $labels.instance }} of job {{ $labels.job }}."

- name: containers
  rules:
  - alert: graylog_down
    expr: absent(container_memory_usage_bytes{name="graylog_graylog_1"})
    for: 30s
    labels:
      severity: critical
    annotations:
      summary: "Graylog down"
      description: "Graylog container is down for more than 30 seconds."

  - alert: hipcms/datastore:test_down
    expr: absent(container_memory_usage_bytes{image="hipcms/datastore:test"})
    for: 30s
    labels:
      severity: critical
    annotations:
      summary: "hipcms/datastore:test down"
      description: "hipcms/datastore:test is down for more than 30 seconds."
      
  - alert: hipcms/angularapp:test_down
    expr: absent(container_memory_usage_bytes{image="hipcms/angularapp:test"})
    for: 30s
    labels:
      severity: critical
    annotations:
      summary: "hipcms/angularapp:test down"
      description: "hipcms/angularapp:test is down for more than 30 seconds."
      
  - alert: hipcms/userstore:test_down
    expr: absent(container_memory_usage_bytes{image="hipcms/userstore:test"})
    for: 30s
    labels:
      severity: critical
    annotations:
      summary: "hipcms/userstore:test down"
      description: "hipcms/userstore:test is down for more than 30 seconds."
      
  - alert: hipcms/thumbnailservice:test_down
    expr: absent(container_memory_usage_bytes{image="hipcms/thumbnailservice:test"})
    for: 30s
    labels:
      severity: critical
    annotations:
      summary: "hipcms/thumbnailservice:test down"
      description: "hipcms/thumbnailservice:test is down for more than 30 seconds."

  
